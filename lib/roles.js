const Employee = require('./employee_class');

class Manager extends Employee {
    constructor (title, first_name, last_name, role_id, manager_id) {
    super(title, first_name, last_name, role_id, manager_id);
    this.
    }
}